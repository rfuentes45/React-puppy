export default function AllPlayers (){ // this is the syntax we use to export this function name
    
    return ( // within the () we can write pure html :) i just learned this
        <header>
            <h1>Puppy Bowl</h1>
        </header>
    );
}

